const contract = require('../contract/contract2');
const document = require('../document/document2.json');

const contractDocument = {
  contract_id: contract.id,
  document_id: document.id,
};

module.exports = contractDocument;
